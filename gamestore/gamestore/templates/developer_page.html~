{% load staticfiles %}
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Quagmire Zone Underground</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous">
        <link rel="stylesheet" type="text/css" href="https://fonts.googleapis.com/css?family=Nosifer">
        <link rel="stylesheet" type="text/css" href="{% static 'quagmire.css' %}">
        <link rel="shortcut icon" href="{% static 'favicon.ico' %}" type="image/x-icon">
    </head>
    <body>
        <header>
            <nav class="navbar">
                <ul class="nav navbar-nav">
                    <li class="title"><a href="/">Quagmire Zone<br>Underground</a></li>
                </ul>
                <div class="navbar-collapse">
                    <ul class="nav navbar-nav navbar-right">
                        <li>
                            <a href="/user/{{ user.username }}" class="btn btn-primary navbar-btn">{{ user.username }}</a>
                        </li>
                        <li>
                            <a href="/developer/{{ user.username }}" class="btn btn-success navbar-btn">Developer page</a>
                        </li>
                        <li>
                            <a href="/logout" class="btn btn-link navbar-btn" role="button">Logout</a>
                        </li>
                    </ul>
                </div>
            </nav>
        </header>
        <div class="container">
            <div class="row">
                <div class="generic username">Developer page - {{ user.username }}</div>
                <div class="generic">Developer options?</div>
                <div class="container">
        	        <div class="row">
        		        <div class="generic">
        			        {% if user.is_authenticated %}
                                {% if user.userextension.isDeveloper %}
                                    This is the developer page, where you can add new games to Quagmire Zone Underground.
                                    <!-- TODO: Should this be in a spoiler-type hidden area that is expanded upon clicking "Add new game?" -->            			            
                                    <form method="POST" action="/developer/{{ user.username }}">
                			            {% csrf_token %}
                			            {{ form.as_p }}
                			            <input class="btn submit" type="submit" value="Submit game" />
            			            </form>
                                    <!-- Add a list of created games here. -->
                                    List of games created by you:
                                    (in progress)
                                {% else %} <!-- TODO: Remove this note? -->
                                    Unfortunately, you do not have the permission to view our developer page. You can become one after reading our <a href="developerinfo" target="/">info page</a>.
                                {% endif %}
            		        {% endif %}
            	        </div>
                    </div>
                </div>
            </div>
        </div>
        <footer class="footer">&copy; Quagmire Zone Underground, 2016</footer>
    </body>
</html>
